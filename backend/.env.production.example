# DeathBox production environment template
# Copy to .env on the server and fill real values

GEMINI_API_KEY=replace_with_real_gemini_key
ELEVENLABS_API_KEY=replace_with_real_elevenlabs_key
ELEVENLABS_VOICE_ID=replace_with_real_voice_id

# Comma-separated frontend origins allowed by CORS
FRONTEND_ORIGINS=https://your-frontend-domain.com

# App runtime
HOST=0.0.0.0
PORT=8000
